cd demo-react=>yarn
before starting react application start json-server with : json-server db.json
React Port :3001
Signin Discrepancies =>the store might not be updated if not sign in store caller returns empty in that case if you do not see the name on the appbar then it indicates sign in not done properly if that happens do click the button 2-3 times after an possible page refresh which will redirect to the page after . 
